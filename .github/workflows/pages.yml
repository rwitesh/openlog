name: Publish Pages

on:
  push:
    branches: ["main"]
    paths: ["webpage/**"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup Pages
        uses: actions/configure-pages@v6
        with:
          enablement: true
      - name: Upload webpage folder only
        uses: actions/upload-pages-artifact@v5
        with:
          path: webpage
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v5
