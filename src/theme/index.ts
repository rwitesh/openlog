/**
 * Consolidated public surface of the theming & preferences system.
 *
 *   tokens:       colors, typography, motion, spacing, radius
 *   preferences:  types, defaults, single DB schema & serialization
 *   context:      Theme + Preferences provider, resolver, and hooks
 */

export * from "./context";
export * from "./preferences";
export * from "./tokens";
